<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.GerenciadorDeArquivos" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primaryColor</item>

        <item name="android:itemBackground">@color/white</item>

        <item name="popupMenuStyle">@style/Popup.Menu</item>
    </style>

    <style name="Theme.GerenciadorDeArquivos" parent="Base.Theme.GerenciadorDeArquivos" />

    <style name="Popup.Menu" parent="@style/Widget.AppCompat.Light.PopupMenu">
        <item name="android:popupBackground">@color/white</item>
    </style>

</resources>